#include <stdio.h>

int main() {
    int soma = 0, num;

    for (int i = 0; i < 3; i++) {
        printf("Digite um número: ");
        scanf("%d", &num);
        soma += num;
    }

    printf("A soma dos números é: %d\n", soma);

    return 0;
}
